# AquaGuardian Frontend Environment Variables
# For local development, leave these empty — Vite proxy handles routing automatically.
# For production (Vercel), set these to your deployed backend URL.

# Backend API base URL (no trailing slash)
VITE_API_BASE_URL=

# Backend WebSocket URL (no trailing slash)  
VITE_WS_URL=
